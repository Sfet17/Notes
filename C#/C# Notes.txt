==============================================================================================
Enumerable.Where Method
=======
Enumerable.Where Method

.Where()
 -> filters a sequence based on a predicate(function to verify each element for a condition)
 -> return an IEnumrable that contains the elements that fit that condition

Example: 
List<string> fruits =
    new List<string> { "apple", "passionfruit", "banana", "mango",
                    "orange", "blueberry", "grape", "strawberry" };

IEnumerable<string> query = fruits.Where(fruit => fruit.Length < 6);

foreach (string fruit in query)
{
    Console.WriteLine(fruit);
}
<<<<<<< Updated upstream

 This code produces the following output:
 apple
 mango
 grape
 
==============================================================================================

==============================================================================================
=======
/*
 This code produces the following output:

 apple
 mango
 grape
*/

.RemoveRange(Object)
 -> tracks the given object in order to delete it from the database when SaveChanges() is applied.
 -> removes items by index
 -> retruns a void value
<<<<<<< Updated upstream
==============================================================================================

==============================================================================================
=======

>>>>>>> Stashed changes
.Remove(Object)
 -> it does the exact same thing
 -> removes an item by value
 -> it returns a bool statement
<<<<<<< Updated upstream
==============================================================================================

==============================================================================================
.ExecuteDelete() // .ExecuteDeleteAsync()
 -> deletes the data from the database directly without traking & selecting it 
Ex: Delete all files that have an Id < 3
	await context.Files.Where(x = x.Id < 3).ExecuteDeleteAsync();

.ExecuteUpdate() // ExecuteUpdateAsync();
 -> executes a proprety based on a condition
Ex: Put the files with the id less than 3 in the backrooms(make them invisible)
	await context.Files.Where(x => x.Id < 3).ExecuteUpdateAsync(s => s.SetPropriety(x => x.IsVisible, false))
==============================================================================================

==============================================================================================
DbContext.Local
 -> it is the place where the queries or the modifications to database are stored before taking full effect on the database(server RAM), or after they are recived.


[  Your Code  ]
      |
      |  (1. Query data)
      v
[  DbContext Memory (RAM)  ] <--- THIS IS ".Local"
|  ----------------------  |      (The Change Tracker's visible list)
|  [ Entity A (Unchanged) ]|
|  [ Entity B (Added)     ]|
|  [ Entity C (Modified)  ]|
|  ----------------------  |
      ^
      |  (2. SaveChanges pushes this data down)
      |  (3. Queries pull data up into here)
      v
[  SQL Database (Disk)]
   [ Row 1 ]
   [ Row 2 ]
   [ Row 3 ]

 -> it's an observable place that is in sync with my acations in real time
 -> it automatically include the Adds, but if i want to Remove something it dosen't show in .Local anymore event tho it is still being tracked. 
				     (use SaveChangesAsync() in order to sync it up no matter the operation)

==============================================================================================


==============================================================================================
Enums
 -> a special "class" used to store constants
 -> by default the first item in a enum(enumartion) starts at zero
 -> can also a differnet index inside of a Enum
Ex: Enum WorkDays
{
 Monday,
 Tuesday,
 Wendesday = 12,
 Thursday,
 Friday
}
Console.Write((int)WorkDays.Wendesday)
	-> : 12
Console.Write((int)WorkDays.Thursday)
	-> : 13

==============================================================================================

==============================================================================================
GroupBy = Enum.TryParse<WidgetGrouping>
		(createWidget.GroupBy, out var groupByValue) ? groupByValue : default

Enum.TryParse(look more into it and exercise using it)
 ->this method returns a boolean value (that's why it's used with the teranry operator, if it's true return groupByValue, if it's not return the default value)
 ->Enum.TryParse<type>
		(string, out var object) ? return if true : return if false


==============================================================================================

==============================================================================================
TryParse vs Parse
 -> it is a method that converts a string into another type of variable like an int,double,Enums, etc.

Parse
 -> throws an exception if it cannot parse the value

TryParse
 -> throws a boolen value, that ididicates if it succeded or not
 -> it's implemented without exceptions so that it's faster

!Use Parse if the convertion is sure to be made, TryParse if not!


==============================================================================================

==============================================================================================
DateTime

==============================================================================================

==============================================================================================
Context.Update()

==============================================================================================



==============================================================================================
!Transient, Scoped and Singleton are "Lifecycles" of an object!
	-> it tells the program how long an object should be alive for

.AddTransient()
  -> creates a copy of the object everytime it is requested
  -> Pros: thread-safe => clean slate every time
  -> Cons: slower, if the object is heavy to create

.AddScoped()
  -> creates an instance once per web request and shares it (like having the same waiter as a restaurant)
  -> Best for: creating databases context and user info

.AddSingleton()
  -> creates the instance once(when the program starts) and dosen't kill it
  -> the object is created the first time it's requested and lives in memory from that point on, every file now shares it
  -> Best for: Caching Services(keep the data in memory), configuraton settings and logging services

==============================================================================================

==============================================================================================
Add() vs AddAsync()
 Add()
  -> this does not send an SQL statement to the database, that happenes when SaveChangesAsync() is called
  -> this does 2 things:
	1.Snapshotting (takes a snapshot of the current values so that it can detect changes later)
	2.State Setting (sets the EntityState to Added)

 AddAsync()
  -> this method exists for Value Generators that need to talk to the database before the entity
	is added to Change Tracker
  -> Hi/Lo Algorithm : a optimization tehnique that reserves 1-100 IDs, so that it can insert
	100 rows without asking the DB for an ID everytime.
		       occasionally it runs out of reserved Ids, so it needs to ask the DB for the next
				cluster. 

!!!Use Add() 99% of the time!!!	
==============================================================================================

==============================================================================================
.ExecuteDelete()
 -> deletes the data from the database directly without traking & selecting it 
==============================================================================================

==============================================================================================
FluentValidator
 -> it's a dotnet library used to define validation rules for different objects in a clean
	readable and reliable way
 SETUP(in Program.Cs || Startup.Cs || HostingExtenstions.cs):
	builder.Services.AddValidatorsFromAssemblyContaining<Validator_Example>();
		-> looks in the enitre folder/projcet that the validator_example is in
			for other validators(basicly any class that inherits AbstractValidator)
	builder.Services AddFluentValidationAutoValidation();
		-> it tells ASP.NET core to look at all the incoming HTTP requests, before it reaches
			the controller endpoint, it checks if the incomming request is valid.
==============================================================================================

==============================================================================================
Generics


==============================================================================================

========================================IQueryable============================================
 -> .NET interface in System.LinQ that represents a collection of data that can be queried, 
	with the query logic translated into antoher language(like SQL)

 -> it creates a part of an SQL querry, making it efficent and reducing load on the server
	since it can access the database ONLY for what is needed(by creating a specific querry with multiple parts)

 -> the query is only executed when iteratting over the data or calling a terminal method
	like FirstOrDefault(), Count(), ToList(), etc.

 -> IQueyable inherits IEnumarable, so that the querry result can me enumarated


==============================================================================================


===============================================AutoMapper===================================================

.ProjectTo<IEntity>(mapper.ConfigurationProvider)
	-> instead of loading full entities in memory and them mapping them,
		this translates the mapping configuration into an SQL SELECT Statement via IQueryable
			(Map<T>() => SELECT * .. || ProjectTo<T>() => SELECT specific columns)
	-> does not work with IQueryable, not IEnumerable or in memory collections

	-> make a high traffic API faster and lighter


============================================================================================================



==============================================AsNoTraking()=================================================
 -> every DbContext has a Change Tracker
				-> an internal dictonary that logs carefully what was modified so that it knows
					what to write into the database when SaveChanges() is applied
 -> AsNoTracking() stops the specific entity to be snapshotted by the Change Tracker.
 -> the main benefit of this is: less memory used, less strain and faster response
 -> mainly used when fetching data from the database that won't be changed(like a GET endpoint)

============================================================================================================



============================================================================================================
Delegates
 -> an object that represents a method
 -> allows to treat a method as a value, assigning a method to a value, passing it to other methods as params,etc.

============================================================================================================


============================================================================================================
Reflection
 -> it's the concept of writting code that can basically inspect various aspects about the code itself

============================================================================================================

============================================================================================================
IServiceScopeFactory
 -> 
============================================================================================================
